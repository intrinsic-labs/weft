# Annotation Update Plan (Archived)

This file previously tracked a broad annotation-doc migration tied to an older project framing.

Current status:
- The historical migration checklist is complete and no longer the active work driver.
- Current roadmap and behavior should be tracked in `docs/STATUS.md` and code in `packages/lsp`.

If a new annotation expansion starts, create a new plan document scoped to:
- parser grammar changes
- analyzer semantics
- LSP diagnostics/completion updates
- docs updates in `docs/STATUS.md` and `docs/GRAMMAR.md`
